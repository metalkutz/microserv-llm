apiVersion: v1
kind: Service
metadata:
  name: microserv-llm
  labels:
    app: microserv-llm
spec:
  selector:
    app: microserv-llm
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP